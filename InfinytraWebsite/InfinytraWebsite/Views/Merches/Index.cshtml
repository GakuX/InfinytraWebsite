@model IEnumerable<InfinytraWebsite.Models.Merch>

@{
    ViewData["Title"] = "Index";
}



@* <p>
    <a asp-action="Create">Create New</a>
</p> *@
@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewItem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageURL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InStock)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categories)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageURL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InStock)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categories)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table> *@


@* <div class="container py-4">
    <h1 class="text-white mb-4">Merch</h1>

    <div class="row g-4">
        @foreach (var item in Model)
        {
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card h-100">
                    <img src="@item.ImageURL" class="card-img-top" style="height:180px; object-fit:cover;" />
                    <div class="card-body">
                        <h5 class="card-title">@item.ItemName</h5>
                        <p class="mb-1 fw-bold">$@item.Price</p>
                        <p class="small text-muted">@item.Categories</p>

                        @if (!item.InStock)
                        {
                            <span class="badge bg-danger">Sold Out</span>
                        }
                    </div>
                    <div class="card-footer bg-transparent border-0 pb-3">
                        <a class="btn btn-sm btn-dark"
                           asp-action="Details"
                           asp-route-id="@item.Id">View</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div> *@



<div class="container">
    <h1 style="color: white">Merch</h1>

    <div class="row">
        @foreach(var item in Model)
        {

          

            <div class="col-12 col-sm-6 col-md-4 col-lg-3">


                <div class="card h-100 w-100 d-flex flex-column">

                    <div class="">
                        <img src="@item.ImageURL" class="card-img-top" style="height:180px; object-fit:cover;" />
                    </div>



                    <div class=" card-body card">

                        <div>
                            @if (item.NewItem)
                            {
                                <h1 style="color:red; font-weight:bold; font-size:30px;">NEW</h1>
                            }
                            else
                            {
                                <h1 style="color:red; font-size: 30px; font-weight:bold">ALMOST GONE</h1>
                            }


                            @if (!item.InStock)
                            {
                                <span class="badge bg-danger">Sold Out</span>
                            }
                            else
                            {
                                <span>.</span>
                            }


                        </div>




                        <div style="padding-top: 5px">

                            <h5 class="card-title">@item.ItemName</h5>
                            <p class="mb-1 fw-bold">$@item.Price</p>
                            <p class="small text-muted">@item.Categories</p>
                        </div>

                        <div class=" bg-transparent border-0 ">
                            <a class="btn btn-sm btn-dark"
                               asp-action="Details"
                               asp-route-id="@item.Id">Item Details</a>
                        </div>
                    </div>


                </div>
                    

@* 
                <p style="color:white;">DEBUG NewItem = @item.NewItem</p>
                <p style="color:white;">DEBUG InStock = @item.InStock</p> *@


               
               
            </div>

           


        }
      @*   <div class="col-5 container "style="background-color: red">

           <input type="text" placeholder="Enter Keywords" />
        </div> *@

    </div>


</div>
